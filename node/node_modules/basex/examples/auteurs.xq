declare option output:method 'jsonml';

let $return :=
	let $fileName := '/var/www/rioultf/svn/m2dnr2i-systeme/code/d3/cours/graph.json'
	let $file := file:read-text($fileName)
	let $json := json:parse($file)
	for $ret in $json//vertices/value
	return <author name="{$ret/name}"></author>
	
	return element{'json'}{$return}
